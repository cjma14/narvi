<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Disk de almacenamiento
    |--------------------------------------------------------------------------
    */
    'disk' => env('IMAGES_DISK', 'public'),

    /*
    |--------------------------------------------------------------------------
    | Calidad AVIF (0-100)
    |--------------------------------------------------------------------------
    */
    'avif_quality' => (int) env('IMAGES_AVIF_QUALITY', 60),

    /*
    |--------------------------------------------------------------------------
    | Ancho máximo por tipo (scaleDown, nunca amplía)
    |--------------------------------------------------------------------------
    */
    'max_width' => [
        'cover' => (int) env('IMAGES_COVER_MAX_WIDTH', 1920),
        'body' => (int) env('IMAGES_BODY_MAX_WIDTH', 1600),
    ],

    /*
    |--------------------------------------------------------------------------
    | Ratio de portada (ancho / alto) y tolerancia relativa
    |--------------------------------------------------------------------------
    | Ejemplo: 3.0 = 3:1. Con tolerancia 0.02 se acepta ±2%.
    */
    'cover_ratio' => (float) env('IMAGES_COVER_RATIO', 3.0),
    'cover_ratio_tolerance' => (float) env('IMAGES_COVER_RATIO_TOLERANCE', 0.02),

    /*
    |--------------------------------------------------------------------------
    | Días antes de purgar archivos físicos
    |--------------------------------------------------------------------------
    */
    'purge_after_days' => (int) env('IMAGES_PURGE_AFTER_DAYS', 7),

    /*
    |--------------------------------------------------------------------------
    | Tamaño máximo de subida (KB)
    |--------------------------------------------------------------------------
    */
    'max_upload_kb' => (int) env('IMAGES_MAX_UPLOAD_KB', 10240),

    /*
    |--------------------------------------------------------------------------
    | Tipos MIME permitidos
    |--------------------------------------------------------------------------
    */
    'allowed_mimetypes' => [
        'image/jpeg',
        'image/png',
        'image/avif',
    ],

];
